"""Ripple: connect a code change to its downstream data consequences.

Inputs: Entire Graph (changed symbols, callers), Entire Checkpoints (intent),
and a Databricks-executed comparison of base vs head over one input snapshot.
"""

__version__ = "0.1.0"
